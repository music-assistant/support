name: Lock Closed Threads

on:
  schedule:
    # Run weekly on Sundays at 4:00 AM UTC
    - cron: '0 4 * * 0'
  workflow_dispatch: {}

permissions:
  issues: write

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
      - uses: dessant/lock-threads@v6
        with:
          github-token: ${{ github.token }}
          issue-inactive-days: 14
          issue-lock-reason: 'resolved'
          issue-comment: >
            This issue has been automatically locked since there has been no
            activity for 14 days after it was closed. If you are still
            experiencing a problem, please open a new issue with updated
            information and reference this one.
          # Only process issues, not PRs (this is a support repo)
          process-only: 'issues'
